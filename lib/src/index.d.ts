import { Rest, middleware } from './rest';
export declare class Restful {
    configure(app: any, options: {
        dirname: String;
    }): void;
}
export { Rest };
export { middleware };
declare var _default: Restful;
export default _default;
